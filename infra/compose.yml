services:
  api-gateway:
    build: ../
    container_name: api-gateway
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=default
    networks:
      - gateway-network

networks:
  gateway-network:
    driver: bridge
